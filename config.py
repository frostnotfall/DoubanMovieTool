#!/usr/bin/env python3
# encoding: utf-8

# 指定token（必选）
token = '***********************************'

# 指定运行方式（二选一）： webhook 或者 polling，如果不指定，则使用 polling 方式
run_mode = 'polling'

# 如果使用 webhook 方式，请指定以下变量
host = 'www.example.com'
